docker run -it \
  -v "${PWD}":"/az" \
  -v $HOME/.netrc:/root/.netrc \
  -v $HOME/.azure:/root/.azure \
  -v /tmp/:/tmp/ \
  --sig-proxy=true \
  --pid=host \
  --rm \
  $extra_params \
  microsoft/azure-cli az "$@"
