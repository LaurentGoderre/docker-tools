docker run -it \
  -v "${PWD}":"/root" \
  -v /tmp/:/tmp/ \
  -v $HOME/.gitconfig:/home/node/.gitconfig \
  -v $HOME/.netrc:/home/node/.netrc \
  --sig-proxy=true \
  --pid=host \
  --rm \
  $extra_params \
  hashicorp/terraform $@
