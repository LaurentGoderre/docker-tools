docker run -it \
  -v "${PWD}":"/tf" \
  -v /tmp/:/tmp/ \
  -v $HOME/.netrc:/root/.netrc \
  -v $HOME/.azure:/root/.azure \
  -w "/tf" \
  --sig-proxy=true \
  --pid=host \
  --rm \
  $extra_params \
  hashicorp/terraform $@
