docker run -it \
  -v "${PWD}":"/tf" \
  -v /tmp/:/tmp/ \
  -v $HOME/.gitconfig:/home/node/.gitconfig \
  -v $HOME/.netrc:/home/node/.netrc \
  -w "/tf" \
  --sig-proxy=true \
  --pid=host \
  --rm \
  $extra_params \
  hashicorp/terraform $@
